package Polymorphism;

public class Intro {
    public static void main(String[] args) {
        // 🔷 Polymorphism in Java.
        // -> Is a Key pillar of oop.
        // -> Many Ways to reprsent something that's known as polymorphism.

        // 🔷 Why Use Polymorphism?
        // Code reusability
        // Extensibility (easy to add new behavior)
        // Reduced complexity through abstraction

        // 🔹 Types of Polymorphism in Java.
        // -> Run Time Polymorphism.
        // -> Compile Time PolyMorphism.

        // 🔹 Key Terms.
        // -> Overloading : Same method name, different parameters.
        // -> Overriding  : Same method name & parameters, but different behavior in subclass
        // -> UpCasting :  	Parent reference refers to child object
        // -> Dynamic method distpatch : JVM decides which method to call at runtime

        // 🔷 What are Annotations?
        // -> Annotations in Java are like labels or metadata attached to code
        // —> they give instructions to the compiler, JVM, or even frameworks

        // 1. You can think of annotations as comments with power.
        // 2. They don’t change the logic directly but affect behavior at compile time or runtime.
    }
}
